// math slice
// has no content, as it has no direct UI component, but is used by all other slices

// canvas slice
export * from './canvas/content';

// variableManager slice
export * from './variableManager/content';

// spreadsheet slice
export * from './spreadsheet/content';

// dataExplorer slice
export * from './dataExplorer/content';

// userInterface slice
export * from './userInterface/content';
